__pycache__/
*.pyc
.pytest_cache/
# patient data — never commit
*.bam
*.bai
*.cram
*.crai
*.vcf*
*.tsv
*.csv
# clinical tables, by DATA extension only: a bare `*clinical*` also matches source files
# (clinical_call.py, test_clinical_call.py) and silently drops them from the commit.
*clinical*.tsv
*clinical*.csv
*clinical*.xlsx
*.log
figures/
