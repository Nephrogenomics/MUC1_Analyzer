"""Détecteurs de paramètres MUC1 depuis un BAM/CRAM patient (Phase 1).

Chaque détecteur expose une fonction pure `detect(...) -> dict` documentant ses
entrées BAM et retournant un fragment de `MUC1Params`. Stubs tant que la validation
sur données réelles n'est pas faite (voir ROADMAP §6 pour les infos requises).
"""
